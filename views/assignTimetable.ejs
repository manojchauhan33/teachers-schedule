<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Assign Timetable</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 10px; }
    input { width: 100%; padding: 5px; }
    h2 { margin-bottom: 20px; }
    button { padding: 10px 20px; margin-top: 20px; }
  </style>
</head>
<body>

  <h2>Assign Timetable for <%= teacher.name %> (<%= teacher.email %>)</h2>

  <form action="/assign-timetable" method="POST">
    <input type="hidden" name="teacherId" value="<%= teacher._id %>">
    <table>
      <thead>
        <tr>
          <th>Day / Period</th>
          <% for (let i = 1; i <= 8; i++) { %>
            <th>Period <%= i %></th>
          <% } %>
        </tr>
      </thead>
      <tbody>
        <% const days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']; %>
        <% days.forEach(day => { %>
          <tr>
            <td><%= day %></td>
            <% for (let i = 1; i <= 8; i++) { %>
              <td>
                <input type="text" name="timetable[<%= day %>][Period <%= i %>][subject]" placeholder="Subject"><br>
                <input type="text" name="timetable[<%= day %>][Period <%= i %>][startTime]" placeholder="Start Time"><br>
                <input type="text" name="timetable[<%= day %>][Period <%= i %>][endTime]" placeholder="End Time"><br>
                <input type="text" name="timetable[<%= day %>][Period <%= i %>][room]" placeholder="Room">
              </td>
            <% } %>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <button type="submit">Save Timetable</button>
  </form>

</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Timetable</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: #000;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .card {
      background: #fff;
      padding: 35px 40px;
      border-radius: 12px;
      width: 100%;
      max-width: 480px;
      box-shadow: 0 12px 25px rgba(0, 0, 0, 0.25);
    }

    h2 {
      color: #8000ff;
      font-size: 24px;
      margin-bottom: 25px;
    }

    label {
      display: block;
      margin: 18px 0 6px;
      font-weight: 500;
      color: #333;
    }

    input, select {
      width: 100%;
      padding: 10px 14px;
      font-size: 16px;
      border-radius: 8px;
      border: 1px solid #ccc;
      outline: none;
    }

    .time-row {
      display: flex;
      gap: 12px;
    }

    .time-row > div {
      flex: 1;
    }

    .buttons {
      display: flex;
      justify-content: flex-end;
      gap: 12px;
      margin-top: 30px;
    }

    .btn {
      padding: 10px 20px;
      font-size: 15px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }

    .btn-cancel {
      background-color: #ddd;
      color: #333;
    }

    .btn-save {
      background-color: #8000ff;
      color: white;
    }
  </style>
</head>
<body>

  <form class="card" action="/assign-timetable" method="POST">
    <h2>Add Time Table Details</h2>

    <input type="hidden" name="teacherId" value="<%= teacher._id %>">

    <label for="day">Day</label>
    <select name="day" id="day" required>
      <% ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'].forEach(day => { %>
        <option value="<%= day %>"><%= day %></option>
      <% }) %>
    </select>

    <label for="lecture">Lecture Number</label>
    <select name="lecture" id="lecture" required>
      <% for (let i = 1; i <= 8; i++) { %>
        <option value="Period <%= i %>">Lecture <%= i %></option>
      <% } %>
    </select>

    <label for="subject">Subject Name</label>
    <input type="text" id="subject" name="subject" required>

    <div class="time-row">
      <div>
        <label for="startTime">Start Time</label>
        <input type="time" id="startTime" name="startTime" required>
      </div>
      <div>
        <label for="endTime">End Time</label>
        <input type="time" id="endTime" name="endTime" required>
      </div>
    </div>

    <label for="room">Room Number</label>
    <input type="text" id="room" name="room" required>

    <div class="buttons">
      <button type="button" class="btn btn-cancel" onclick="window.history.back()">Cancel</button>
      <button type="submit" class="btn btn-save">Save</button>
    </div>
  </form>

</body>
</html>
